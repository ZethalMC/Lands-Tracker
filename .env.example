DISCORD_TOKEN=
CLIENT_ID=
# Optional: set to a guild ID to register commands instantly to just that
# guild (fast local/dev testing). Leave blank to register commands globally
# (production, applies to every guild the bot is in, ~1hr propagation).
GUILD_ID=
# Discord user ID allowed to run /shutdown.
OWNER_ID=
